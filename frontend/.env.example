# Leave unset for local dev — Vite's dev-server proxy (vite.config.ts) handles
# routing /api and /socket.io to http://localhost:4000 automatically.
#
# Set this when building for production, to the deployed backend's origin
# (no trailing slash, no /api suffix):
# VITE_BACKEND_URL=https://your-backend.up.railway.app
